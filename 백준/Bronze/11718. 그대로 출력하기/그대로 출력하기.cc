#include <iostream>
#include <vector>
#include <string>
#include <sstream>

using namespace std;
int main()
{
	vector<string> Words;
	string Word;
	while (getline(cin, Word))
		cout << Word << endl;


}